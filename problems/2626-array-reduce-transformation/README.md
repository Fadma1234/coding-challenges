# 2626. Array Reduce Transformation

Problem: Implement an array reducer without using `Array.prototype.reduce`.

Files
- `solution.js` — implementation exported as `arrayReduce`.
- `test.js` — example tests for the three provided cases.

Run tests:

```bash
node problems/2626-array-reduce-transformation/test.js
```
